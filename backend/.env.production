# Production Environment Variables for Render
NODE_ENV=production
PORT=10000

# MongoDB Atlas Connection (already configured)
MONGODB_URI=mongodb+srv://admin:Ayra1357@cluster0.5u65it4.mongodb.net/medical_app?retryWrites=true&w=majority&appName=Cluster0

# JWT Secret (generate a strong secret for production)
JWT_SECRET=your-super-secure-production-jwt-secret-key-change-this-in-render

# CORS Origins (update with your actual Netlify URL)
ALLOWED_ORIGINS=https://clinical.netlify.app,https://YOUR_NETLIFY_SITE_NAME.netlify.app